# AGENTS.md

本文件约束 `WutheringWaves-Toolkit` 目录及其所有子目录中的开发工作。Phase 0 工程基础已经建立；修改功能、数据或部署流程前应先阅读同目录下的《网站技术方案.md》。

## 项目目标

构建一个可部署到 GitHub Pages 的《鸣潮》玩家工具站。首要功能是伤害与循环计算器：用户可配置共鸣者、武器、声骸、合鸣效果、敌人及队伍，并在时间轴上安排技能，得到逐次伤害、总伤害、DPS、增益覆盖率与资源变化结果。

## 核心原则

- 计算可信优先于界面丰富；任何公式或机制实现都必须可追溯、可测试。
- 模拟内核必须与 Vue 组件、Pinia、DOM 和浏览器存储解耦，保持纯 TypeScript、确定性和可复现。
- 游戏资料、计算规则、用户配置与展示文案分层存放，不在组件中硬编码倍率或效果。
- 时间统一使用整数毫秒 `timeMs`；同一时间戳的事件必须使用显式优先级和稳定序号排序。
- 内部数值使用完整精度，仅在展示层舍入。百分比在模型内统一使用小数（`0.2` 表示 20%），除非字段名明确包含 `PercentPoints`。
- 所有持久化/分享数据必须包含 `schemaVersion`，通过迁移函数读取旧版本。
- GitHub Pages 是纯静态环境；不得让核心功能依赖服务器、私密密钥或运行时写入文件。
- 不复制 `Endaxis` 的游戏数据或实现；可参考其分层思路，但应按《鸣潮》机制独立建模。
- 游戏数据以库街区《鸣潮》WIKI（`https://wiki.kurobbs.com/mc/home`）为主要来源；鸣潮 BWiki 与 `https://wikiwiki.jp/w-w/` 是固定社区交叉验证源。其他社区资料只用于补足三者未提供的隐藏公式和实测机制。“主要来源”不免除实体级核对、版本标注和冲突记录。

## 技术栈

- Vue 3、TypeScript、Vite，组件统一使用 Composition API 与 `<script setup lang="ts">`。
- Pinia 管理编辑器与用户项目状态；Vue Router 使用适配 GitHub Pages 的路由策略。
- Vitest 负责单元与集成测试；需要组件测试时再引入 Vue Test Utils。
- ESLint 与 Prettier 负责静态检查和格式化；包管理器以项目锁文件为准，不混用。
- 生产构建产物为 `dist/`，由 GitHub Actions 发布到 GitHub Pages。

## 目录职责

现有和后续目录遵守以下边界；尚未需要的领域目录不应仅为匹配结构图而提前创建：

- `src/app/`：应用启动、路由、全局插件与布局。
- `src/features/`：按业务功能组织页面、组件、store 与 composable。
- `src/domain/`：游戏领域类型、枚举和值对象，不依赖 UI。
- `src/data/`：版本化游戏静态数据及数据索引。
- `src/simulator/`：场景编译、事件引擎、机制处理、公式与结果投影。
- `src/shared/`：无业务归属的通用组件与工具。
- `src/workers/`：Web Worker 入口和消息协议。
- `tests/fixtures/`：可审查的标准配装、时间轴与金标准结果。
- `scripts/`：数据校验、转换及构建辅助脚本。

依赖方向必须保持为：`features/app -> simulator -> domain/data`；`domain` 和 `simulator` 不得反向导入 `features`、Vue 组件或 Pinia store。

## 实现规范

- 优先定义领域类型和输入/输出契约，再实现 UI。禁止以 `any` 绕过核心计算类型；外部数据先按 `unknown` 接收并校验。
- 简体中文显示名以库街区《鸣潮》WIKI 的官方名称为准，保留官方用字与技能树层级；BWiki、WikiWiki 和其他社区站点只用于别名及数值交叉验证，不能覆盖官方中文名。伤害动作按“技能分类·官方技能名·动作名”展示（如 `常态攻击·风羽为刃·重击`），避免不同共鸣者的同名动作产生歧义。
- 共鸣者、武器、声骸、合鸣、技能与具名效果使用其官方中文名称的无声调小写拼音作为稳定字符串 ID，词语之间以短横线分隔（如 `qiangu-fuliu`、`liufeng-zaiyu`）；不以英文译名、本地化显示名称或上游 CMS ID 作为主键。已发布 ID 不因展示层补充技能树前缀而改动。没有中文专名的技术枚举继续使用英文。代码领域命名统一使用 `Resonator`，仅在泛化 UI 文案中使用“角色”。
- 计算器只接受满级养成：共鸣者固定 90 级且技能固定满级，武器固定 90 级，五星声骸固定 +25 且主动技能固定最高阶。玩家在配装侧只调整共鸣链、武器谐振阶与五个声骸位的实际属性数值；敌方等级和抗性仍属于独立场景参数。
- 数据项至少记录 `gameVersion`、来源说明或来源链接、维护时间；不确定机制必须显式标记，不能伪装为精确值。
- 不在生产应用运行时抓取 WIKI。获得自动化访问许可后，数据更新才可由受控脚本生成候选数据，再经人工审阅、schema 校验和金标准测试后提交为版本化静态数据。
- WIKI 的目录 ID、词条 ID 和在线版本号仅作为来源追踪字段，不作为项目稳定主键；项目主键必须由仓库维护并通过映射表关联来源。
- WIKI 词条中的关键数值常位于富文本 HTML 表格和自然语言中。解析器输出一律视为 `candidate`，不得未经审阅直接进入 `src/data/versions/`。
- 共鸣者、武器、声骸和合鸣实体应保存实体级 `sourceRefs`、核对时间与 `verificationStatus`；不要求为每个倍率重复保存字段级来源。公式或帧数据若来自非官方实测，仍需注明具体来源与验证方法。
- 被动效果应使用声明式条件/效果模型；只有声明式表达能力不足时才添加具名机制处理器，避免任意脚本式配置。
- 时间轴动作是用户意图，必须先编译成命中、Buff、资源等运行时事件，再进入模拟器；UI 不直接计算伤害。
- Phase 2 默认采用估算时序：动作持续时间按伤害段数与集中配置的每段时长推导，多段命中在该区间内均匀分布。估算值必须标记为 `estimated`，不伪装成实测帧数；未来获得可复核实测时可按动作覆盖。
- 时间轴允许三类重叠：启动后独立执行的召唤型声骸、由触发器派生的协同攻击，以及切换角色时尚未结束并会继续执行的原角色技能。其他动作不得仅因时间戳交叉就默认可重叠。
- 闪避作为显式时间轴动作取消当前前台动作。取消时刻及之后未执行的命中、Buff 和资源事件不再处理；已在动作开始时结算的效果不回滚。
- 同时间戳事件顺序按 ADR 0003 集中实现：预清理→切换/取消与动作开始/消耗→开始效果→共享快照命中→命中后效果/回复→端点到期。
- 模拟结果需保留伤害分解，包括攻击/生命/防御基数、倍率、增伤、易伤、抗性、防御、暴击期望等因子，便于审计。
- 预期伤害与确定暴击/不暴击模式分别实现；不要通过随机数得出默认结果。
- 大型模拟在 Web Worker 中执行；worker 消息只传可结构化克隆的数据。
- 新增用户可见文案时使用 i18n key；至少保证简体中文资料完整，并为英文预留结构。
- UI 图标、品牌和装饰图形使用可维护的 SVG；角色、武器、声骸和合鸣等游戏插图使用 WebP/AVIF，遵守资源许可并在需要时记录归属；不要提交来源不明的大体积游戏资源。
- 无障碍是完成标准的一部分：交互控件可键盘操作、有可读标签；时间轴必须提供非拖拽的精确编辑方式。

## 测试与验证

- 每个伤害公式、叠层规则、触发条件、资源变化和事件优先级都应有单元测试。
- 修复计算错误时先添加能复现错误的测试，再修复实现。
- 关键角色循环使用 fixture 做金标准测试；浮点比较使用明确容差，不对整段结果随意更新快照。
- 数据提交必须通过 schema 校验、ID 唯一性、引用完整性和数值范围校验。
- 数据更新还必须保存原始响应的内容哈希或等价审计信息，并生成字段级 diff；官方页面更新不得自动覆盖人工修订而不告警。
- UI 变更至少验证桌面与移动布局；时间轴变更还需验证拖放、键盘输入、缩放和重叠动作。
- 完成改动前运行 `format:check`、`lint`、`type-check`、`test`、`data:validate` 和 `build`；若环境导致某项无法运行，在交付说明中明确指出。

## GitHub Pages 与安全

- Vite `base` 必须根据仓库站点路径配置，禁止假定总是部署在域名根目录。
- 不提交密钥、令牌、个人数据或受版权限制的抓取原始包。
- 仅访问无需登录的公开 WIKI 页面；不得把调研时观察到的站点内部接口视为受支持的公共 API。遵守站点条款、robots、限速与缓存要求，不得绕过鉴权、验证码或访问控制。自动化采集与定时同步必须在确认许可后实施。
- 分享链接中的用户数据需要做大小限制和 schema 校验；解码失败应安全回退，不执行其中任何代码。
- 若采用 history 路由，必须同时提供 Pages 的回退方案；默认优先 hash 路由以降低部署复杂度。

## 改动纪律

- 保持提交和改动聚焦，不顺手重构无关模块。
- 新增依赖前说明用途，并优先选择体积小、维护活跃、浏览器兼容且不依赖后端的方案。
- 修改公共 schema、公式或事件顺序属于高风险变更，必须同步更新文档、迁移和测试。
- 方案与实际实现发生偏差时，同一改动中更新《网站技术方案.md》，避免文档失真。
