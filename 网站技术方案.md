# 鸣潮伤害计算器技术方案

> 状态：Phase 0、Phase 1 已实现；Phase 2 进行中
>
> 最近核对：2026-08-12
>
> 部署目标：GitHub Pages 纯静态站点
>
> 主要数据源：[库街区《鸣潮》官方 WIKI](https://wiki.kurobbs.com/mc/home)

## 1. 项目定位

本项目是面向《鸣潮》玩家的开源 Web 伤害计算器，采用“配装 + 时间轴 + 战斗模拟”一体化的单一全屏工作区。项目不再定位为工具箱，不提供工具目录首页或独立图鉴产品；共鸣者、武器、声骸与合鸣资料只作为计算器内的配置数据。

核心使用流程：

1. 选择游戏版本、队伍与敌人。
2. 配置最多三名共鸣者的等级、技能、共鸣链、武器、五枚声骸及合鸣效果。
3. 在时间轴上按时间戳放置普攻、重击、共鸣技能、共鸣解放、声骸技能、变奏/延奏等动作。
4. 编译时间轴并运行确定性事件模拟。
5. 展示总伤害、DPS、逐共鸣者/逐技能占比、逐次命中、资源曲线、Buff 覆盖及每次伤害的公式分解。
6. 将方案保存在本地，或通过文件/压缩链接导入导出。

本项目不是游戏客户端、自动操作工具或联网战斗分析器；首版不需要账号系统和后端服务。

## 2. 设计目标与非目标

### 2.1 设计目标

- **可信**：同样输入始终得到同样输出，结果能够逐项解释。
- **可维护**：新共鸣者与新版本主要通过数据文件及少量具名机制扩展。
- **高交互性**：桌面端支持拖拽、缩放、滚动和键盘精确编辑；窄屏保留数值输入路径。
- **离线优先**：首次加载后核心功能可在无网络环境使用，用户方案保存在浏览器本地。
- **静态可部署**：构建产物直接发布到 GitHub Pages，不依赖运行时服务器。
- **性能可控**：模拟内核保持纯 TypeScript 边界，先建立性能基准，必要时再迁入 Web Worker。

### 2.2 首版非目标

- 不做多人实时协作、云同步或账号登录。
- 不模拟走位、网络延迟、敌人 AI、操作失误等全部实战变量。
- 不把 Monte Carlo 随机暴击作为默认计算方式；默认展示期望值，并提供确定暴击/不暴击模式。
- 不一开始覆盖全部历史共鸣者和全部边缘机制；先建立可验证的纵向切片。

## 3. 技术选型

| 范畴       | 建议方案                              | 选择理由                                                  |
| ---------- | ------------------------------------- | --------------------------------------------------------- |
| 框架       | Vue 3 + TypeScript                    | Composition API 适合复杂交互与强类型领域模型              |
| 构建       | Vite                                  | 静态构建快，GitHub Pages 配置简单                         |
| 状态       | Pinia                                 | 管理配装、时间轴、偏好与持久化状态                        |
| 路由       | Vue Router（默认 hash）               | 避免 Pages 子路径刷新 404                                 |
| UI         | Headless/轻量组件 + 项目主题          | 避免大型组件库绑死时间轴交互；具体库在原型后决定          |
| 结果可视化 | 当前使用轻量 DOM/SVG；复杂图表待评估  | 不为未实现图表预先引入大型依赖                            |
| 拖拽       | Pointer Events                        | 时间轴需要自定义播放头吸附、缩放与并发容量                |
| 校验       | Zod                                   | 已用于游戏数据、项目存档和导入载荷                        |
| 本地存储   | IndexedDB + localStorage              | IndexedDB 保存多项目，localStorage 保存当前 ID 和轻量偏好 |
| 分享       | 版本化 JSON 文件                      | 当前已实现；URL 压缩分享延后                              |
| 计算线程   | 主线程纯 TS 模拟器；Worker 待基准验证 | 保持边界可迁移，不在无性能证据时增加协议复杂度            |
| 测试       | Vitest + Vue Test Utils（按需）       | 公式、引擎与组件测试统一在 Vite 生态                      |
| 质量       | ESLint + Prettier + vue-tsc           | 统一风格并保证模板类型安全                                |
| CI/CD      | GitHub Actions + Pages artifact       | PR 验证与部署自动化                                       |

首版建议使用 npm 并提交 `package-lock.json`。Node.js 版本固定在当前维护中的 LTS 大版本，并通过 `.nvmrc`/`engines` 与 CI 保持一致。

## 4. 总体架构

```text
Vue 页面与组件
      │ 用户编辑
      ▼
Pinia Project Store ──────► IndexedDB / JSON 导入导出
      │ SimulationInput（纯数据）
      ▼
Scenario Compiler
      │ RuntimeEvent[]
      ▼
Deterministic Event Engine
      │ SimulationLog + Snapshots
      ▼
Result Projectors
      │ SimulationResult
      ▼
结果面板 / 图表 / 警告 / 伤害明细
```

参考 `Endaxis` 的关键经验是把“用户编辑的时间轴”和“模拟器实际处理的事件”分开：编辑器只描述玩家意图；编译器将一个技能展开为动作开始、多个命中、效果施加、资源增减和动作结束；引擎按稳定顺序执行；投影层将详细日志整理为 UI 所需图表。这种分层能避免 Vue 组件承担战斗规则，也方便单独测试公式。

### 4.1 主要数据源调研结论（2026-08-08）

官方入口为 [库街区《鸣潮》WIKI](https://wiki.kurobbs.com/mc/home)，页面说明其包含共鸣者图鉴、声骸图鉴、攻略与活动内容。站点是 Vue/Vite 单页应用，公开目录按“游戏图鉴”组织。调研时确认以下计算器相关目录：

| 类别                                                                     | 官方目录 ID | 调研时条目数 | 列表结构                            | 详情中的有效信息                                 |
| ------------------------------------------------------------------------ | ----------: | -----------: | ----------------------------------- | ------------------------------------------------ |
| [共鸣者](https://wiki.kurobbs.com/mc/catalogue/list?fid=1099&sid=1105)   |      `1105` |           60 | 星级、属性标签、图片、词条引用      | 基础属性等级页、技能说明/倍率、共鸣链、固有技能  |
| [武器](https://wiki.kurobbs.com/mc/catalogue/list?fid=1099&sid=1106)     |      `1106` |          120 | 星级、武器类型标签、图片、词条引用  | 等级面板、主/副属性、谐振 1–5 阶效果             |
| [声骸](https://wiki.kurobbs.com/mc/catalogue/list?fid=1099&sid=1107)     |      `1107` |          198 | COST/等级、合鸣关联、图片、词条引用 | COST、声骸技能、星级倍率、冷却与对应敌人         |
| [合鸣效果](https://wiki.kurobbs.com/mc/catalogue/list?fid=1099&sid=1219) |      `1219` |           34 | 名称、图片、词条引用                | 套装档位效果、触发条件、叠层与持续时间           |
| [敌人](https://wiki.kurobbs.com/mc/catalogue/list?fid=1099&sid=1158)     |      `1158` |          205 | 分类标签、图片、词条引用            | 基础介绍与位置；计算所需等级/防御/抗性不保证齐全 |

上述数量是 2026-08-08 的一次调研快照，包含当时已展示的预告内容，不代表某个游戏版本内的可用实体数，也不是代码常量。调研时观察到目录与详情载荷带有上游版本字段；这些字段可作为变更线索，但属于站点内部实现，不构成稳定 API 契约。

WIKI 当前展示数据可概括为两层：

1. 目录层提供条目 ID、名称、星级/COST/属性等筛选标签、封面和词条链接。
2. 详情层提供模块与组件；大多数面板、倍率和被动描述位于 `basic-component`/`tabs-component` 的 HTML 表格与富文本中。角色卡有少量结构化 `role` 字段，但伤害计算字段没有统一 API schema。

因此，该 WIKI 是名称、描述、显示面板与公开倍率的**主要来源**，但不是可直接使用的计算数据库，也不能仅凭 HTML 自动解析结果宣称计算准确。时间轴首版不要求逐动作实测命中帧：暂按每段 `50ms` 估算动作持续时间，首段伤害在动作开始时产生，后续段每隔 `50ms` 产生。任意动作重叠、动作长度裁剪和同时间戳结算顺序按 ADR 0003 执行；隐藏取整和部分敌方抗性仍需可复核资料。估算时序必须在结果中显式标记。若 WIKI、游戏内文本与版本公告冲突，以当前目标游戏版本内可复核的信息为准，并保留冲突记录。

### 4.2 与 Endaxis 实现的差异

| 维度           | Endaxis（终末地）现状                                                     | 本项目（鸣潮）要求                                                      | 架构影响                                                                                    |
| -------------- | ------------------------------------------------------------------------- | ----------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- |
| 队伍与在场角色 | 4 名干员轨道，已有 `switchEvents`/controlled operator                     | 3 名共鸣者；切人直接连接协奏能量、变奏和延奏                            | 切换必须是一等事件，并在编译时验证协奏条件                                                  |
| 装备           | 武器 + armor/gloves/2 accessories + gear set                              | 武器 + 5 枚声骸；总 COST/同名装配等合法性、主副词条、主声骸位、合鸣档位 | 不能复用 Endaxis gear slot/set 模型；需独立 Echo roll、合法性校验与数据驱动的 Sonata 推导器 |
| 角色成长       | level/promotion/potential、技能/天赋等级                                  | 等级/突破、共鸣链、技能树节点、技能等级                                 | 成长 schema 和效果解锁条件需重新设计                                                        |
| 资源           | SP、终结技能量、连携/队伍资源等                                           | 共鸣能量、协奏能量、耐力和每角色独有回路资源                            | 使用可扩展的 typed resource registry，避免写死单一 gauge                                    |
| 动作分类       | basic/battle/combo/ultimate/finisher/dive                                 | 普攻、重击、空中、闪避反击、共鸣技能、解放、变奏、延奏、声骸等          | 标签需支持“一动作多标签”和伤害类型归属，不能照搬联合类型                                    |
| 队伍触发       | Link、Combo、干员/装备 TriggerRegistry                                    | 变奏/延奏、协同攻击、治疗/护盾/异常触发、在场/后台条件                  | Trigger 必须带 source/owner/active-character/target 范围和触发间隔                          |
| 元素/反应      | 物理状态、自然/寒冷等伤害与终末地反应系统                                 | 冷凝、热熔、导电、气动、衍射、湮灭；异常效应属于不同机制                | 不移植 Endaxis 的 infliction/reaction 状态机，先按鸣潮实测独立建模                          |
| 伤害公式       | `OperatorStatus`、敌方 susceptibility/res shred、反应和 `DamageBreakdown` | 《鸣潮》的攻击/生命/防御缩放、增伤、加深、抗性、防御、暴击及特殊乘区    | 保留可审计 pipeline 思路，替换全部乘区定义、取整和标签过滤规则                              |
| 时间模型       | 现有代码混用秒值/realTime，并有多轨编译与命中展开                         | 统一整数 `timeMs`，另记录来源帧率与实测帧点                             | 编译器边界显式转换；事件键固定为 `(timeMs, priority, sequence)`                             |
| 数据实现       | 每个干员/武器/装备为强类型 TS 模块，效果声明式并辅以具名机制              | 官方 WIKI 主要为富文本 HTML，且版本持续更新                             | 增加 raw → candidate → normalized ETL；规范化后仍采用强类型静态模块                         |
| 数据来源       | 仓库内手工维护，含大量针对机制的测试                                      | 官方 WIKI 为主要来源，社区/实测补足隐藏机制                             | 每个字段需来源与验证状态；同步不能等同于发布                                                |
| 运行方式       | 主线程中的编译/模拟与 Pinia 投影较紧密，部分类型仍有 `any`                | 主线程运行纯 TS 内核，依赖边界预留 Worker 迁移能力                      | 禁止 simulator 依赖 store/UI；达到基准阈值后再定义 worker protocol                          |
| 结果           | 详细日志、Buff/资源投影、伤害归因与大量金标准测试                         | 还需配装合法性、合鸣推导、协奏循环和来源置信度警告                      | 结果增加 loadout diagnostics 与 data provenance                                             |

应复用的是 Endaxis 的**思路**：领域数据 → 时间轴编译 → 稳定事件队列 → 状态/效果处理 → 伤害分解 → UI 投影，以及针对触发、叠层和金标准的大量测试。不能复用的是终末地专属枚举、装备槽、属性、SP/反应公式和具体角色数据。Endaxis 当前若干宽松的 `any` 与 UI/store 耦合也不应成为新项目的起点。

### 4.3 模块依赖约束

```text
app/features ──► simulator ──► domain
      │              │            ▲
      └──────────────┴──────► data

shared 不得依赖具体 feature
domain 不依赖 Vue、Pinia、浏览器 API
simulator 不依赖组件与 store
```

## 5. 当前项目结构

```text
WutheringWaves-Toolkit/
├─ .github/
│  └─ workflows/
│     ├─ ci.yml
│     └─ deploy-pages.yml
├─ docs/
│  ├─ adr/                       # 重要架构决策记录
│  ├─ mechanics/                 # 机制与公式依据
│  ├─ research/                  # 阶段调研和复核结论
│  └─ data-maintenance.md
├─ public/
│  └─ manifest.webmanifest
├─ scripts/
│  ├─ validate-data.ts
│  └─ wiki/                       # 官方/社区 Wiki 快照与候选处理
├─ src/
│  ├─ app/
│  │  ├─ router/
│  │  └─ App.vue
│  ├─ assets/                    # SVG 品牌资源与 WebP 游戏插图
│  ├─ features/
│  │  ├─ calculator/
│  │  │  ├─ components/              # effects/inspector/projects/results/shell/skills/team/timeline/ui
│  │  │  ├─ composables/             # UI 与模拟器的组合逻辑
│  │  │  ├─ layout/                  # 确定性动作泳道排布
│  │  │  ├─ persistence/             # IndexedDB、schema、JSON 迁移
│  │  │  ├─ stores/                  # project/projectLibrary/timeline/timelineViewport
│  │  │  ├─ timeline/                # schema、迁移和动作持续时间模型
│  │  │  ├─ styles/                  # 壳层样式及按业务拆分的 panels/timeline 子模块
│  │  │  └─ views/
│  ├─ domain/
│  │  ├─ combat.ts
│  │  └─ loadout.ts
│  ├─ data/
│  │  ├─ versions/
│  │  │  └─ vX_Y/
│  │  │     ├─ sources.ts            # 实体级来源
│  │  │     ├─ entities.ts           # 首批武器/声骸/合鸣与共鸣者面板
│  │  │     ├─ resonators/           # 按共鸣者拆分的动作与效果
│  │  │     ├─ defaults/             # 纵向切片默认配装
│  │  │     └─ phaseOne.ts           # 兼容 re-export 入口
│  │  ├─ schemas/
│  │  └─ wiki/
│  ├─ simulator/
│  │  ├─ calculation/
│  │  ├─ compiler/
│  │  └─ simulate.ts
│  ├─ shared/
│  │  └─ i18n/
│  ├─ main.ts
│  └─ vite-env.d.ts
├─ tests/
│  └─ fixtures/                   # 可审查金标准输入/结果
├─ AGENTS.md
├─ 网站技术方案.md
├─ index.html
├─ package.json
├─ tsconfig.json
└─ vite.config.ts
```

目录按当前实际责任列出，不为目标架构预创建空文件夹。2026-08-12 的结构审计已删除旧首页/图鉴/数据状态、旧共享壳层、空迁移目录和 Worker 预留目录，详见 [`docs/research/project-structure-audit.md`](docs/research/project-structure-audit.md)。3.5 纵向数据已按来源、实体、共鸣者动作和默认配装拆分，`phaseOne.ts` 只维持现有 import 的兼容性。时间轴的 viewport、播放头、动作 Pointer 和键盘交互已下沉到独立 composable；文档 store 与视口 store 已分离，schema、迁移和动作模型位于独立纯 TypeScript 模块。时间轴与侧栏面板 CSS 分别使用小型聚合入口，内部按对应组件职责拆分。

## 6. 核心领域模型

### 6.1 配装与项目

```ts
interface CalculatorProject {
  schemaVersion: number
  gameVersion: string
  id: string
  name: string
  team: TeamLoadout
  enemy: EnemyConfig
  timeline: TimelineDocument
  settings: SimulationSettings
  updatedAt: string
}

interface TeamLoadout {
  members: ResonatorLoadout[] // schema 强制长度为 1–3，且 resonatorId 不重复
}

interface ResonatorLoadout {
  resonatorId: string
  sequence: number
  weapon: WeaponLoadout
  echoes: EchoSetLoadout
}

interface EchoSetLoadout {
  main: MainEchoLoadout
  secondary: [EchoStatSlot, EchoStatSlot, EchoStatSlot, EchoStatSlot]
  sonataId: string
}

interface MainEchoLoadout extends EchoStatSlot {
  echoId: string
  phantom?: boolean
}

interface EchoStatSlot {
  cost: 1 | 3 | 4
  mainStat: StatRoll
  subStats: StatRoll[]
}

interface EnemyConfig {
  level: number
  resistances: Partial<Record<ElementType, number>>
}
```

计算器不提供养成等级调节：共鸣者固定 90 级、全部可升级技能固定满级；武器固定 90 级，仅保留 1–5 阶谐振；五星声骸固定 +25，首位主动技能固定最高阶。共鸣者只开放共鸣链 0–6，声骸只开放各位置实际属性。每个五星声骸最多保存五条独立副词条，不能用一个无类型的“副词条合计”替代。

声骸配置固定为五个位置，但只有首位主声骸关联具体 `echoId`、技能等级和声骸技能；其余四个属性位不要求选择声骸名称，只录入 COST、主词条和副词条的具体数值。合鸣效果由用户选择 `sonataId`，档位与数值来自版本化数据，不能把“2 件/5 件”写死在引擎中。总 COST 和词条合法性由版本化规则校验；由于四个属性位不保存具体声骸 ID，不对它们执行同名声骸校验。敌人不绑定具体名称或图鉴条目，Phase 1 仅由用户配置敌人等级和各属性抗性，防御区根据等级与已验证公式计算。

### 6.2 时间轴文档

```ts
interface TimelineAction {
  id: string
  resonatorId: string
  actionId: string // 稳定技能/动作 ID
  startTimeMs: number
  variant?: string
  params?: Record<string, string | number | boolean>
}

interface TimelineDocument {
  durationMs: number
  snapMs: number
  tracks: Array<{
    resonatorId: string
    actions: TimelineAction[]
  }>
}
```

动作条目不直接保存最终伤害，也不重复保存可从版本化动作时序策略推导出的命中时间。Phase 2 的默认策略为：每段暂按 `50ms` 计算动作持续时间，首段位于动作开始，后续命中每隔 `50ms` 产生；单段动作同样在动作开始时命中。策略和估算参数集中配置并标记为 `estimated`，以后可被单个动作的实测时序覆盖。

动作是否占用当前角色操作由版本化数据声明，不根据技能名猜测：

- 所有技能动作均可重叠，编辑器不以角色占用或执行方式阻止放置。
- 同一角色轨内重叠的动作按区间装箱到多个子行，不互相遮挡；排布结果必须确定且可测试。
- 角色切换、召唤与协同等机制仍可由动作数据表达，但不参与编辑器的重叠合法性判断。

用户直接拖动动作右缘或输入结束时间来裁剪动作长度，不单独放置闪避动作。编译器删除裁剪点之后尚未执行的命中、结束时 Buff 和后续资源事件；已经在动作开始时结算的 Buff、资源变化和其他事件不回滚。

时间轴将“切换到某共鸣者”保存为用户动作；编译器依据该时刻的协奏状态决定是否派生变奏/延奏事件。用户不能单独放置不满足切换前提的变奏/延奏。任一时间点只能有一名在场共鸣者；同时间切换的排序与合法性必须显式验证。

### 6.3 运行时事件

建议的最小事件集合：

- `ACTION_START` / `ACTION_END`
- `HIT` / `DAMAGE`
- `BUFF_APPLY` / `BUFF_EXPIRE` / `STACK_CHANGE`
- `RESOURCE_CHANGE`（协奏能量、共鸣能量、特殊回路资源）
- `RESONATOR_SWITCH`
- `INTRO_TRIGGER` / `OUTRO_TRIGGER`
- `COORDINATED_ATTACK`
- `HEAL` / `SHIELD_CHANGE`（用于触发相关效果，即便首版结果不重点展示）

事件排序键为 `(timeMs, priority, sequence)`，优先级在 ADR 0003 集中定义并写测试。同一时间戳先处理早于当前时刻到期的效果，再处理切换、动作开始与资源消耗、`action-start` 效果、共享状态快照下的全部命中，然后处理 `hit-after` 效果与资源回复，最后让结束于当前时刻的效果到期。裁剪点后的事件在此前编译阶段已经移除。因此 Buff 结束时间包含端点，同时间戳的命中不会因稳定序号不同而享受到刚由另一次命中触发的 Buff。

### 6.4 运行时状态

- 队伍状态：当前在场共鸣者、队伍级 Buff、协奏关系。
- 共鸣者状态：面板属性、动态 Buff、共鸣/协奏/特殊资源、冷却、触发计数。
- 敌人状态：等级、防御、各属性抗性、减抗、易伤、状态效果。抗性乘区使用三段公式：负抗 `1 - RES/2`、`0 ≤ RES < 0.8` 时 `1 - RES`、`RES ≥ 0.8` 时 `1/(1+5RES)`。前两段为交叉验证，高抗段为社区共识并保留待实测标记；特殊完全免疫独立建模。
- 全局状态：当前时间、事件队列、稳定序号、战斗日志、诊断警告。

状态变更只能由事件处理器完成；伤害计算读取命中时刻的快照，防止 UI 或数据加载顺序影响结果。

## 7. 计算模型

精确公式应在 `docs/mechanics/` 中按游戏版本记录来源与验证样例。代码层不要只返回最终数字，而应返回结构化分解：

```ts
interface DamageBreakdown {
  scalingBase: number
  skillMultiplier: number
  flatDamage: number
  damageBonusMultiplier: number
  deepenMultiplier: number
  critMultiplier: number
  defenseMultiplier: number
  resistanceMultiplier: number
  specialMultiplier: number
  finalDamage: number
}
```

概念流程（仅用于划分待验证乘区，不代表最终《鸣潮》公式或运算顺序）：

```text
（缩放基础值 × 技能倍率 + 待确认的固定项）
× 伤害加成区
× 伤害加深/易伤区
× 暴击区
× 防御区
× 抗性区
× 特殊独立乘区
= 最终伤害
```

实现要求：

- 支持攻击、防御、生命等不同缩放属性；是否存在多属性混合缩放和固定伤害项按实际数据建模，不提前假定。
- 支持普攻、重击、空中攻击、共鸣技能、共鸣解放、声骸、协同攻击等标签筛选。
- Buff/被动用“条件 + 效果 + 持续时间 + 叠层策略 + 触发器”描述。
- 明确区分静态面板、动态战斗属性、敌方状态和本次命中特有修正。
- 暴击模式至少包括 `expected`、`alwaysCrit`、`neverCrit`；`expected` 使用期望乘数。
- 每次命中记录所有生效来源，结果面板可回答“这个乘区为什么是该数值”。
- 对尚未确认的取整时机和快照/动态结算输出诊断标记；同时间戳优先级按 ADR 0003 执行。

## 8. 时间轴编辑器设计

### 8.1 桌面布局

- 顶部：项目名称、项目切换/新建/导入导出、撤销/重做等项目级操作。
- 左侧：固定宽度工具栏与可关闭面板；面板在队伍配置和技能库之间切换。
- 中间：固定共鸣者标签列、共享时间尺、播放头、三条技能轨道与 5 条效果轨道。
- 右侧：与左侧同宽的工具栏与可关闭面板；面板在技能状态和计算结果之间切换。

### 8.2 必要交互

- 点击技能库条目将动作添加到当前可视窗口，然后在轨道中拖动或删除。
- 内部保存整数毫秒。拖动时默认自由定位，接近播放头时吸附；检查器与键盘提供精确微调。
- 支持 30/60/120 秒时长、水平滚动、宽范围缩放和“适应到最后一个动作”。
- 同一共鸣者最多 4 个动作同时存在，确定性泳道排布会按并发数扩展轨道高度。
- 诊断提示：资源不足、冷却未完成、共鸣者不在场、前置条件不满足；动作重叠本身不属于冲突。
- 动作条不常驻显示技能名称或时间，通过选中、悬浮提示和右侧检查器识别；命中点是编译结果的只读投影。
- 移动端提供动作列表和数字时间输入，不能强迫用户进行精细拖拽。

当前由 composable 在主线程投影纯 TypeScript 模拟结果。实际性能基准超过交互预算时，再引入带递增 `requestId` 的 Worker 协议，避免慢结果覆盖新编辑。

## 9. 数据来源、组织与版本管理

### 9.1 来源优先级

1. **主要来源**：库街区《鸣潮》WIKI，用于公开名称、星级、类型、等级面板、技能/武器/声骸/合鸣描述和公开倍率。
2. **官方补充**：游戏内文本、官方公告与版本说明，用于校正版本变更和 WIKI 尚未同步的字段。
3. **可复核实测**：快照、隐藏公式与敌人抗性，以及未来可选的真实命中时序覆盖。实测数据必须记录测试方法、环境和样本；未实测的时序使用统一估算策略并明确标记。
4. **固定社区交叉验证**：[鸣潮 BWiki](https://wiki.biligame.com/wutheringwaves/首页) 与[鸣潮 Wiki*](https://wikiwiki.jp/w-w/) 用于系统核对通用机制、满级面板、技能倍率、五星武器、声骸和合鸣结构，并记录别名；简体中文显示名仍以官方 Wiki 为准，存在冲突时保留差异和判定依据。
5. **其他社区资料**：仅作为官方 Wiki、BWiki 与 WikiWiki 均缺失时的线索，必须注明来源等级。

“主要来源”不意味着内容已结构化、永远正确或适合自动再分发。调研中观察到的站点内部接口不是已承诺的公共 API。建立抓取或定时同步前需确认站点条款、robots、自动化访问与内容再利用许可；获准后的同步脚本仍须限速、缓存并支持离线 fixture，CI 不直接依赖线上服务。

### 9.2 数据流水线

```text
WIKI 公开目录/词条
        │ fetch（开发脚本，非浏览器运行时）
        ▼
raw manifest（URL/ID/在线版本/时间/hash，不默认提交完整响应）
        │ parse HTML + normalize labels
        ▼
candidate JSON（解析候选值 + 原文片段定位 + parser warnings）
        │ 人工字段级审阅 + 第二来源补充
        ▼
reviewed normalized data
        │ schema/ref/range/golden tests
        ▼
src/data/versions/<game-version>/ + 构建索引
```

获得自动化访问许可后可新增：

```text
scripts/wiki/
├─ fetch-catalogue.ts            # 获取目录快照，限速并缓存
├─ fetch-entry.ts                # 按变更版本获取公开详情
├─ parse-components.ts           # 解析 basic/tabs/role 组件
├─ normalize-candidates.ts       # 映射官方名称到领域字段
└─ diff-source-version.ts        # 输出字段级变更报告

data-sources/kuro-wiki/
├─ manifest.json                 # 来源 ID、URL、版本、hash、核对时间
├─ mappings.json                 # 官方词条 ID ↔ 项目稳定 ID
└─ candidates/                   # 可选的待审阅中间产物
```

生产站点只读取已提交的规范化静态数据，不向 WIKI 或其内部服务发请求。这样避免 CORS/可用性变化、上游 HTML 编辑破坏解析、用户访问时的隐私与限流问题，也保证旧项目可按指定游戏版本重算。

### 9.3 来源元数据

每个计算相关实体或可独立变化的字段组至少带：

```ts
interface SourceRef {
  source: 'kuro-official-wiki' | 'official-game' | 'official-notice' | 'measurement' | 'community'
  url: string
  entryId?: string
  catalogueId?: number
  upstreamVersion?: string
  retrievedAt: string
  contentHash?: string
  note?: string
}

type VerificationStatus =
  'candidate' | 'reviewed-primary-source' | 'measured' | 'cross-checked' | 'disputed'
```

项目稳定 ID 使用中文正式名称的无声调小写拼音，并以短横线分词（如 `yangyang`、`qiangu-fuliu`、`liufeng-zaiyu`）。它不能使用英文译名、官方目录卡片 ID、UUID 或词条 ID，因为译名可能调整，上游 CMS 标识也可能重建或迁移。映射表负责关联。

### 9.4 版本化领域数据

游戏数据属于最容易失真的部分，采用“版本化源数据 + 构建索引”：

- 每个版本目录只记录该版本的有效数据，或通过基础数据加补丁生成完整快照。
- 数据 schema 校验稳定 ID、枚举、数值范围和跨文件引用。
- 技能数据描述动作时长、命中偏移、倍率、标签、资源与声明式效果。
- 复杂机制放在 `simulator/mechanics/<mechanic-id>.ts`，数据只引用受控的 mechanic ID。
- 共鸣者、武器、声骸和合鸣按实体记录来源、核对日期、验证状态和游戏版本，不要求每个倍率重复字段级链接；公式和实测机制单独记录验证方法。本地化名称与 ID 分离。
- 数据更新通过独立 PR，CI 输出缺失引用、重复 ID 和异常范围报告。
- 官方 WIKI 变更先生成 candidate diff；解析失败、字段消失或数值突变必须阻止自动合并。

游戏资源的抓取、转换和再分发必须确认许可。官方 WIKI 页面可公开访问不等同于授权镜像其图片或批量再分发内容。若许可不明，首版使用自制占位图、远端链接的合规方案或仅存计算必需的事实性文本与数值；资源抓取脚本不接入默认构建。

## 10. 状态、存档与分享

Pinia store 分为：

- `projectStore`：当前项目的配装、敌人和模拟输入。
- `projectLibraryStore`：多项目索引、当前项目、创建/删除/重命名与自动保存状态。
- `timelineStore`：动作、撤销/重做、时长与编辑边界。
- `timelineViewportStore`：缩放、可视时间范围和当前共鸣者槽；不进入项目持久化文档。

游戏数据从版本化静态模块读取，模拟结果由 composable 计算；当前不为只读数据或结果另建 store。

持久化策略：

- IndexedDB：多个 `CalculatorProject` 及元数据。
- localStorage：当前项目 ID 与轻量界面偏好。
- JSON 文件：完整、可审查的导入导出格式。
- URL hash 压缩分享尚未实现；当前通过 JSON 文件传递项目。

导入流程为“解码 → schema 校验 → 版本迁移 → 业务引用校验 → 用户确认”，任何一步失败都不得污染当前项目。自动保存采用 debounce，并保留最近一次可恢复版本。

## 11. GitHub Pages 部署

仓库项目页通常位于 `https://<owner>.github.io/WutheringWaves-Toolkit/`。Vite 必须设置正确的 `base`：

```ts
export default defineConfig({
  base: process.env.GITHUB_ACTIONS ? '/WutheringWaves-Toolkit/' : '/',
})
```

若未来仓库更名或绑定自定义域名，应改为从环境变量读取，而不是把路径散落在代码中。资源引用统一经过 Vite 导入或 `import.meta.env.BASE_URL`。

部署工作流：

1. PR/推送运行安装、格式检查、lint、类型检查、测试和构建。
2. `main` 分支通过验证后上传 `dist/` 为 Pages artifact。
3. 使用官方 Pages action 部署，并设置最小 `pages: write`、`id-token: write` 权限。
4. 通过 `concurrency` 取消过期部署，避免旧构建覆盖新构建。

使用 History 路由，根路由 `/` 直接打开单一计算工作区。历史工具/图鉴路径只作兼容重定向，不再是产品导航结构；生产构建同步生成与 `index.html` 一致的 `404.html`，用于 GitHub Pages 仓库子路径下的直接访问与刷新回退。

## 12. 性能与可访问性

性能预算建议：

- 首屏不加载全量高清图片和全部图表模块。
- 路由与大型功能按需加载；游戏数据按版本/类别拆包。
- 静态图片转换为 WebP/AVIF，并提供尺寸信息避免布局偏移。
- 典型 120 秒、3 共鸣者循环的主线程重算目标低于 100 ms（目标值，Phase 2 基准测试后确认）；若持续超出交互预算，再迁入 Worker。
- 时间轴只渲染视口附近元素；缩放/拖动使用 `requestAnimationFrame` 合并更新。

可访问性要求：

- 所有拖拽操作有按钮/键盘等价操作。
- 颜色不是唯一状态表达方式；伤害属性和警告同时使用文本/图标。
- 焦点样式清晰，弹窗可正确管理焦点，表单有 label 和错误说明。
- 支持减少动画偏好，图表提供文字摘要或表格视图。

## 13. 测试策略

测试金字塔：

1. **数据校验**：schema、唯一 ID、引用完整性、版本元信息。
2. **公式单测**：防御、抗性、暴击、增伤/加深、属性缩放和取整边界。
3. **机制单测**：持续时间、叠层、刷新/替换、快照、触发冷却和资源变化。
4. **事件引擎单测**：同时间戳顺序、稳定性、取消事件、递归触发上限。
5. **编译器单测**：一个时间轴动作展开为正确的命中与效果事件。
6. **金标准集成测试**：使用人工验证的共鸣者配装与循环，核对每次伤害和总伤害。
7. **组件/E2E**：配装、拖拽/精确输入、保存、导入、分享和移动端关键路径。

首批 fixture 应刻意覆盖：单段直伤、多段攻击、Buff 在命中前/后生效、切人触发延奏/变奏、协同攻击、声骸/合鸣效果、资源不足警告、敌人不同抗性。

## 14. 安全、稳定性与免责声明

- 全站不需要秘密密钥；任何需要密钥的数据服务都不应在浏览器端直连。
- 导入内容和 URL 载荷设置解压后大小、数组长度、时间轴时长与事件数上限，防止内存耗尽。
- i18n 文本和用户项目名按纯文本渲染，禁止不受控 `v-html`。
- 模拟器设置最大事件数与最大触发深度，发现循环触发时终止并给出诊断。
- 页面明确标注粉丝项目、与库洛游戏无隶属关系、计算结果仅供参考，并提供数据来源与错误反馈入口。
- 对游戏名、角色图标等商标与素材遵循适用的社区/版权政策。

## 15. 分阶段实施路线

### Phase 0：工程基础

当前状态：**已实现，历史 UI 已被后续产品调整替换**。

- 已建立 Vue 3 + TypeScript + Vite、带 GitHub Pages 回退的 History 路由、Pinia、i18n、Vitest、ESLint/Prettier、CI 和 GitHub Pages 部署。
- 已建立官方 Wiki 来源 manifest、Zod schema、合成 fixture、正文快照/候选处理脚本和数据校验命令。
- 早期的工具首页、图鉴模板、数据状态页和浅色公共站点外壳已从产品边界中移除；当前仓库只保留单一全屏伤害计算器。
- UI 图标、品牌和装饰图形使用 SVG；共鸣者、武器、声骸和合鸣等游戏插图使用 WebP/AVIF。中文是默认界面语言，不依赖远程 CJK Webfont。

验收结果：应用可针对 GitHub Pages 仓库子路径构建，本地质量命令与部署工作流已建立；来源清单覆盖共鸣者、武器、声骸、合鸣效果与敌人。

### Phase 1：单共鸣者纵向切片

当前状态：**已实现**。3.5 纵向切片已包含秧秧全部伤害动作的满级倍率与官方 Wiki 中文名称、实体级来源、完整伤害乘区、稳定动作编译、声明式武器/声骸/合鸣/共鸣链效果、五星 +25 声骸合法性、逐次伤害分解及带来源和逐命中预期的金标准 fixture。backup 中的效果说明只作为候选输入；迁移的游戏插图已转换为 WebP。Phase 2 使用“首段即时命中 + 后续每 `50ms` 一段”的显式估算模型；动作重叠、长度裁剪和同时间戳结算已由 ADR 0003 确定。

- 目标游戏与数据版本确定为《鸣潮》3.5；首批领域数据、公式来源、人工算例和界面版本标识均以 3.5 为适用范围。
- 共鸣者固定 90 级且技能满级，武器固定 90 级，五星声骸固定 +25/最高技能阶；玩家只调整共鸣链、武器谐振阶和声骸实际属性数值。
- 首组配置确定为共鸣者“秧秧”、武器“千古洑流”、首位主声骸“飞廉之猩”和合鸣效果“啸谷长风”；其余四个声骸位置只录入 COST 与词条数值。
- 录入上述共鸣者、武器、主声骸技能和合鸣数据；敌人不录入具体名称，由用户配置等级与各属性抗性。
- 已完成配装面板、完整乘区公式、声明式效果状态、动作编译和逐次伤害结果。
- 已建立含双社区来源、完整输入和逐命中预期值的外部锚定金标准 fixture，并覆盖叠层、过期、触发时点、免疫和声骸合法性边界。

验收：用户能配置单共鸣者并得到可展开、可解释的正确伤害结果。

### Phase 2：时间轴 MVP

当前状态：**进行中**。已实现估算动作区间、首段即时/后续 `50ms` 间隔命中、自动子行、4 路并发输入边界、长度裁剪、同时间戳共享快照、自由拖动/播放头吸附/缩放、毫秒精确输入、撤销/重做、多项目 IndexedDB 保存和 JSON 导入导出。应用是单一全屏深色计算工作区：左侧固定宽度面板切换队伍/技能库，中间使用共享标尺和滚动的时间轴，右侧同宽面板切换技能状态/计算结果。效果区固定为三名共鸣者增益、全队增益、敌方减益共 5 行；声明式效果通过 `self`、`team`、`enemy` 指定目标。

- 已完成：多轨时间轴、精确时间编辑、播放头吸附、缩放/滚动、撤销/重做、效果区间、本地多项目和 JSON 导入导出。
- 待完成：三人队伍配置重构、可变资源/冷却/前置条件、第二名共鸣者数据、时间轴交互测试与性能基准。
- Worker 是基准验证后的可选优化，不是 Phase 2 验收的预设必要条件。

验收：可稳定编辑并计算一段单共鸣者完整循环，刷新后恢复。

### Phase 3：队伍机制

- 3 人队伍、切人、协奏能量、变奏/延奏、队伍 Buff、协同攻击。
- 声骸主动技与合鸣、动态敌方状态。
- 共鸣者/技能/伤害类型图表和 Buff 覆盖率。

验收：至少一个人工验证的完整队伍循环通过金标准测试。

### Phase 4：内容与分享

- 扩充共鸣者、武器、声骸、合鸣和敌人数据。
- URL 压缩分享、方案模板、移动端优化、PWA 离线缓存。
- 建立数据贡献文档与版本迁移流程。

验收：版本更新能通过数据 PR 完成，旧存档可迁移，新方案可可靠分享。

## 16. 首轮待确认决策

以下问题不阻塞工程基础，但应在对应 Phase 开始前形成 ADR：

1. 官方 WIKI 自动化访问与内容/图片再利用许可、同步频率及维护责任人。
2. Phase 1 目标数据版本已确定为 3.5；首个金标准配置已确定为“秧秧 + 千古洑流 + 飞廉之猩 + 啸谷长风”；其余四个声骸位使用 COST/词条数值，敌人使用无名称的等级/抗性参数。
3. Phase 2 已通过 ADR 0003 确定首段即时/后续 `50ms` 间隔的估算时序、动作重叠、长度裁剪和同时间戳事件顺序；编辑器首版限制同一共鸣者最多 4 路并发。
4. 各伤害乘区、取整节点、快照机制的可靠依据。
5. Phase 2 已通过 ADR 0004 选择原生 Pointer Events，并提供毫秒数值输入作为非拖拽替代。
6. Phase 2 首轮已通过 ADR 0004 选择 IndexedDB 保存多项目、localStorage 保存当前项目 ID/轻量偏好，并使用版本化 JSON 导入导出。
7. 是否计划自定义域名；GitHub 仓库名已固定为 `WutheringWaves-Toolkit`。
8. 官方 WIKI 已能保存正文快照和可检索候选文本；尚需决定结构化富文本解析器只覆盖哪些稳定表格，以及哪些机制字段始终人工录入/实测。

## 17. 完成定义（Definition of Done）

一个计算功能只有同时满足以下条件才算完成：

- 领域数据与公式来源已记录，并标注适用游戏版本。
- 模拟器实现不依赖 UI，输入相同则输出完全可复现。
- 结果包含足够的伤害分解与生效来源，用户可审计。
- 正常、边界和失效条件均有测试；相关金标准未回退。
- 桌面和移动端均可操作，拖拽功能有精确输入替代方式。
- 导入导出 schema、迁移、i18n 和文档已同步更新。
- `format:check`、`lint`、`type-check`、`test`、`build` 全部通过。
